{
  "name": "@try-lerna/booking-ui",
  "version": "0.1.0",
  "scripts": {
    "build": "react-scripts build",
    "start": "PORT=3000 react-scripts start",
    "test": "CI=true react-scripts test"
  },
  "browserslist": {
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ],
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ]
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "proxy": "http://localhost:8080",
  "dependencies": {
    "@try-lerna/logger": "^1.0.0",
    "@try-lerna/validator": "^1.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1"
  }
}
